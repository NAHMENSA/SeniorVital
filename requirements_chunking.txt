# Dependencies for SeniorVital knowledge chunking and RAG pipeline

# Core chunking framework
langchain>=0.3.0
langchain-experimental>=0.3.0

# Local HuggingFace embeddings integration (replaces langchain-community HuggingFaceEmbeddings)
langchain-huggingface>=0.1.0

# Local embeddings (free, no API key required)
sentence-transformers>=3.0.0

# Vector store (used in Fase 5 integration)
chromadb>=0.5.0

# Token counting and utilities
tiktoken>=0.7.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Environment and testing
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional: only if you want to use OpenAI instead of local embeddings
# langchain-openai>=0.2.0
# openai>=1.0.0

# Cross-encoder reranker (used in Fase 5 integration)
# transformers>=4.40.0
# torch>=2.3.0
